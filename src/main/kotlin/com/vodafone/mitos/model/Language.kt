package com.vodafone.mitos.model

/**
 * Source language of a graph node. Drives the visual palette in the renderer
 * (FR-18) and the choice of analyzer in the service.
 */
enum class Language {
    JAVA,
    SPRING,
    JSP,
    JAVASCRIPT,
    UNRESOLVED,
}
